import React from "react";

const Header = () => {
  return (
    <header
      style={{
        backgroundColor: "#565867",
        color: "white",
        padding: "15px",
        textAlign: "center",
        fontSize: "1.5rem",
        fontWeight: "bold"
      }}
    >
    </header>
  );
};

export default Header;
